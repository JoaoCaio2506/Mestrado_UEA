# Production URL of the "When chat message received" (Chat Trigger) node
# in the Mestrado_UEA n8n workflow. Copy this file to .env and fill it in —
# .env is gitignored so the real URL never gets committed.
#
# Leave empty to keep using the local simulated diagnosis response instead
# of calling n8n.
VITE_N8N_WEBHOOK_URL=
